# llox

A collection of interpreter and compiler for lox.

## Setup

```bash
# Install git hooks (formatting on commit)
sh scripts/setup-hooks.sh

# Build
cd llox/build
cmake ..
make
```

## Docs

```bash
cd gh-pages && npm install && npm run dev
```
